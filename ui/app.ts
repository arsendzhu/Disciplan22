export { AppShell } from "@/components/navigation/AppShell";

export const appModuleNotes = {
  purpose:
    "DisciPlan-style UI entry barrel. The live web app routes still live under Next.js app/.",
  routeGroups: ["@/app/(auth)", "@/app/(tabs)", "@/app/api"],
};
